export enum Variant {
  primary = 'primary',
  secondary = 'secondary',
  tertiary = 'tertiary',
}

export enum Shape {
  sharp = 'sharp',
  smooth = 'smooth',
  rounded = 'rounded',
}

export enum Size {
  small = 'small',
  medium = 'medium',
  large = 'large',
}
